---
sidebar_position: 2
title: "2. Grundbausteine des Internets"
description: "In diesem Abschnitt machen wir einen Rückblick auf die Protokolle und Technologien, auf denen das Internet basiert."
---

import {
    InlineCode,
    Table,
    TableBody,
    TableCell,
    TableColumn,
    TableHeader,
    TableRow, Text
} from "@mittwald/flow-react-components";

## 1. Kurze Vorschau auf das Transmission Control Protocol/Internet Protocol (TCP/IP) Modell

* Das **TCP/IP Modell** ist ein Protokollmodell, das die Kommunikation im Internet beschreibt.
* Es besteht aus vier Schichten, die jeweils spezifische Aufgaben in der Datenübertragung übernehmen.
* Die einzelnen Schichten bauen aufeinander auf
* Jede Schicht erfüllt tiefgehendere Anforderungen an die Kommunikation als die darunterliegende Schicht.
* Dieser Aufbau ermöglicht bspw. Übertragungsprotokolle unabhängig von den darunterliegenden Technologien zu entwickeln. Bspw. ist dies der Grund, warum die gleichen höher liegenden Protokolle sowohl für Ethernet als auch für WLAN verwendet werden können.
* Das Modell wurde von der DARPA (Defense Advanced Research Projects Agency)entwickelt und stellt derzeit das Standardmodell für die Kommunikation im Internet dar.
* Alternativ exstiert noch das ISO OSI Modell, welches jedoch in der Praxis kaum noch verwendet wird.
* Weitere Behandling findet im Modul "Rechnernetze" statt

### Die 4 Schichten des (TCP/IP) Modells

* **Schicht 1: Netzwerkzugriffsschicht**
    * Beinhaltet die physikalische Übertragung von Bits und die Sicherung der fehlerfreien Übertragung
    * Beantwortet die Fragen:
        * Was muss mit dem Kabel oder der Luft passieren, um 0 und 1 zu übertragen?
        * Was muss passieren, um sicherzustellen, dass die 0 und 1 korrekt ankommen?
    * Beispiele: Ethernet, WLAN

* **Schicht 2: Internetschicht**
    * Verantwortlich für das Routing von Datenpaketen zwischen Netzwerken
    * Beantwortet die Frage:
        * Wie finden die Daten den Weg von einem Gerät zum richtigen Empfänger - auch über viele Zwischenstationen?
    * Beispiele: IP (Internet Protocol), ICMP (Internet Control Message Protocol)

* **Schicht 3: Transportschicht**
    * Gewährleistung der zuverlässigen Übertragung von Datenrahmen
    * Gewährleistet die zuverlässige Übertragung von Datenströmen
    * Verwaltung von Sitzungen zwischen Anwendungen
    * Beantwortet die Fragen:
        * Wie stellen wir sicher, dass alle Datenpakete vollständig, richtig und in der richtigen Reihenfolge ankommen?
        * Wie behalten wir den Überblick, wer mit wem spricht, welche Datenpakete zu welchem Gespräch gehören und wo wir im Gespräch gerade sind?
    * Beispiele: TCP, UDP

* **Schicht 4: Anwendungsschicht**
    * Formatierung und Übersetzung von Daten für die Anwendung
    * Bereitstellung von Diensten für Anwendungen
    * Beantwortet die Fragen:
        * Wie müssen die Daten aufbereitet sein, damit Sender und Empfänger sie verstehen können?
        * Welche Information/Anfrage möchte die Anwendung senden und welche Antwort erwartet sie?
    * Beispiele: HTTP, FTP, SMTP, DNS

### Relevant für Web Technologien

* Besonders relevante Protokolle (sortiert nach Schichten) für Web Technologien sind:
    * **IP** (Internet Protocol) - Vermittlungsschicht
    * **TCP** (Transmission Control Protocol) - Transportschicht
    * **HTTP** (Hypertext Transfer Protocol) - Anwendungsschicht
    * **HTTPS** (HTTP Secure) - Anwendungsschicht (sichere Variante von HTTP)
    * **DNS** (Domain Name System) - Anwendungsschicht (Übersetzung von Domainnamen in IP-Adressen)

### Internet Protocol (IP)

* **IP** ist das grundlegende Protokoll für die Adressierung und das Routing von Datenpaketen im Internet.
* Es ermöglicht die Kommunikation zwischen Geräten (über verschiedene Netzwerke hinweg).
* **IP-Adressen** sind eindeutige Identifikatoren für Geräte im Internet.
* Jede Webseite, jede Web Applikation und jeder Dienst im Internet oder Intranet ist über eine IP-Adresse erreichbar.
* **IP-Pakete** enthalten die Quell- und Ziel-IP-Adressen sowie die Nutzdaten.
* Dabei muss unterschieden werden zwischen IPv4 (32-Bit Adressen) und IPv6 (128-Bit Adressen).
* **IPv4** ist das aktuell noch am weitesten verbreitete Protokoll
* Aufgrund der begrenzten Anzahl an Adressen wird zunehmend auf **IPv6** umgestellt.
* IPv4 ermöglicht etwa 4,3 Milliarden eindeutige Adressen, während IPv6 eine nahezu unbegrenzte Anzahl (340 Sextillionen, 36 Nullen) an Adressen bereitstellt.
* Aufgrund der begrenzten Zahl von IPv4 Adressen, gibt es Adressbereiche, die für private Netzwerke reserviert sind, um die Adressknappheit zu umgehen.
* Diese Adressen sind nicht im Internet erreichbar, sondern nur innerhalb eines privaten Netzwerks.
* Um diese privaten Adressen im Internet zu nutzen, wird Network Address Translation (NAT) verwendet, um sie in öffentliche IP-Adressen umzuwandeln.
* Dazu wird ein Router benötigt, der die Adressen übersetzt und die Pakete entsprechend weiterleitet.

### Domain Name System (DNS)

* **DNS** ist ein hierarchisches System zur Übersetzung von Domainnamen in IP-Adressen.
* Nutzer geben in der Regel Domainnamen (z.B. www.example.com) ein, die dann in die entsprechende IP-Adresse aufgelöst werden.
* Die Relevanz für Web Technologien liegt darin, dass DNS die Benutzerfreundlichkeit des Internets verbessert, indem es die Verwendung von leicht merkbaren Domainnamen anstelle von numerischen IP-Adressen ermöglicht.
* IPv4 Adressen sind in der Regel noch einigermaßen merkbar, aber IPv6 Adressen sind für Menschen kaum noch zu merken.
    * IPv4 Adressen werden numerisch in 4 Segmenten von 0 bis 254 unterteilt dargestellt
        * Beispiel: 127.0.0.1 (localhost, die eigene Maschine, auch Loopback-Adresse genannt)
    * IPv6 Adressen werden hexadezimal in 8 Segmenten von 0 bis FFFF unterteilt dargestellt
        * Beispiel: 2001:0db8:85a3:0000:0000:8a2e:0370:7334
* Um eine Domain in eine IP-Adresse aufzulösen, sendet der Client eine Anfrage an einen DNS-Server.
* Der DNS-Server sucht die zugehörige IP-Adresse in seiner Datenbank und sendet sie zurück an den Client.
* Wenn der DNS-Server die IP-Adresse nicht kennt, kann er die Anfrage an andere DNS-Server weiterleiten, bis die Adresse gefunden wird.

### Transmission Control Protocol

* **TCP** ist **verbindungsorientiert**
* vor dem Datentransfer muss eine logische Verbindung zwischen Sender und Empfänger aufgebaut werden (Handshake)
* Das Hauptziel ist die zuverlässige, geordnete und fehlerfreie Übertragung von Datenpaten.
* Es ergänzt das Internet Protocol (IP), das für die Adressierung und das Routing von Datenpaketen verantwortlich ist, aber selbst keine Garantie für die Zustellung oder Reihenfolge der Pakete bietet.
* **TCP-Pakete** enthalten Sequenznummern, um die Reihenfolge der Pakete zu verfolgen, und Prüfsummen, um die Integrität der Daten zu überprüfen.
* **TCP** ist wichtig für Web Technologien, da es die Grundlage für die meisten Internetprotokolle bildet, einschließlich HTTP und HTTPS.

### Hypertext Transfer Protocol (HTTP)

* **HTTP** ist das Protokoll, das für die Übertragung von Webseiten und Web Applikationen im Internet verwendet wird.
* Es ermöglicht den Austausch von Daten zwischen Webservern und Webbrowsern.
* **HTTP-Anfragen** werden von einem Client (z.B. Webbrowser) an einen Server gesendet, um Ressourcen wie HTML-Dokumente, Bilder oder Skripte anzufordern.
* **HTTP-Antworten** enthalten die angeforderten Ressourcen sowie Statuscodes, die den Erfolg oder Misserfolg der Anfrage anzeigen.
* **HTTP** ist ein zustandsloses Protokoll, was bedeutet, dass jede Anfrage unabhängig von vorherigen Anfragen behandelt wird.
* HTTP wird im Folgenden noch ausführlicher behandelt, da es das zentrale Protokoll für dieses Modul ist.

### Hypertext Transfer Protocol Secure (HTTPS)

* **HTTPS** ist die sichere Variante von HTTP, die eine verschlüsselte Kommunikation zwischen Client und Server ermöglicht.
* Es verwendet Transport Layer Security (TLS), um die Daten während der Übertragung vor Abhören und Manipulation zu schützen.
* **HTTPS** ist besonders wichtig für Web Applikationen, die sensible Daten übertragen, wie z.B. Online-Banking oder E-Commerce-Websites.
* Die Verwendung von HTTPS ist heute Standard und wird von den meisten modernen Webbrowsern und Webservern unterstützt und aktiv gefordert.
* TLS (Transport Layer Security) ist der Nachfolger von SSL (Secure Sockets Layer), SSL wird heute nicht mehr verwendet.
* Das Thema HTTPS und TLS wird im Kapitel "Web Application Security" tiefgehender behandelt.

## 2. HTTP im Detail

* HTTP funktioniert nach einem klaren zustandslosen Prinzip:
* Der Client sendet eine Anfrage an den Server.
* Der Server verarbeitet die Anfrage und sendet eine Antwort zurück.

* Eine HTTP-Anfrage besteht aus:
    * **Methode**: Gibt die Art der Anfrage an (z.B. GET, POST, PUT, DELETE).
    * **URL**: Die Adresse der angeforderten Ressource.
    * **HTTP-Version**: Gibt die verwendete Version des HTTP-Protokolls an (z.B. HTTP/1.1).
    * **Header**: Zusätzliche Informationen über die Anfrage (z.B. Content-Type, User-Agent).
    * **Body**: Nur bei POST- oder PUT-Anfragen enthalten, enthält die zu sendenden Daten.

* Eine HTTP-Antwort besteht aus:
    * **HTTP-Version**: Gibt die verwendete Version des HTTP-Protokolls an (z.B. HTTP/1.1).
    * **Statuscode**: Ein dreistelliger Code, der den Erfolg oder Misserfolg der Anfrage anzeigt (z.B. 200 für Erfolg, 404 für nicht gefunden).
    * **Statusnachricht**: Eine kurze Beschreibung des Statuscodes.
    * **Header**: Zusätzliche Informationen über die Antwort (z.B. Content-Type, Content-Length).
    * **Body**: Enthält die angeforderten Daten (z.B. HTML-Dokument, JSON-Daten).

### HTTP Methoden

* Die wichtigsten HTTP-Methoden in der Praxis sind:
    * **GET**: Fordert eine Ressource an (z.B. eine Webseite).
        * Wird verwendet, um Daten vom Server abzurufen.
        * Keine Daten im Body der Anfrage, da es sich um eine reine Abfrage handelt
        * Die Antwort enthält die angeforderten Daten im Response Body.
        * GET-Anfragen sollten idempotent sein, d.h. sie sollten keine Seiteneffekte haben und mehrfaches Ausführen sollte stets die gleiche Antwort liefern, solange die Resource sich nicht (durch andere Requests) verändert hat
    * **POST**: Sendet Daten an den Server (z.B. Formulardaten).
        * Wird verwendet, um Daten zu erstellen oder zu aktualisieren.
        * Kann Daten im Body der Anfrage enthalten, die an den Server gesendet werden.
    * **PUT**: Aktualisiert eine Ressource auf dem Server.
        * Wird verwendet, um eine Ressource vollständig zu ersetzen.
        * Kann Daten im Body der Anfrage enthalten, die die aktualisierte Ressource darstellen.
    * **PATCH**: Aktualisiert teilweise eine Ressource auf dem Server.
        * Wird verwendet, um eine Ressource teilweise zu aktualisieren.
        * Kann Daten im Body der Anfrage enthalten, die die Änderungen an der Ressource darstellen.
    * **DELETE**: Löscht eine Ressource auf dem Server.
        * Wird verwendet, um eine Ressource zu löschen.
        * Keine Daten im Body der Anfrage, da es sich um eine Löschoperation handelt.
  * Weitere HTTP-Methoden, die in der Praxis häufig beobachtet werden, sind:
    * **HEAD**: Fordert nur die Header einer Ressource an, ohne den Body.
        * Wird verwendet, um Metadaten über eine Ressource abzurufen.
        * Nützlich, um Informationen wie den Content-Type oder die Last-Modified-Zeit zu erhalten, ohne die gesamte Ressource herunterzuladen.
        * In der Praxis wird HEAD oft nicht manuell verwendet, sondern von Browsern oder Caches automatisch genutzt, um Informationen über Ressourcen zu erhalten.
    * **OPTIONS**: Fordert die unterstützten HTTP-Methoden und -Header für eine Ressource an.
        * Wird verwendet, um herauszufinden, welche HTTP-Methoden und -Header für eine bestimmte Ressource unterstützt werden.
        * Nützlich für CORS (Cross-Origin Resource Sharing) Anfragen, um zu überprüfen, ob eine Ressource von einer anderen Domain aus zugänglich ist.
        * Kann auch verwendet werden, um die Serverkonfiguration zu überprüfen.
        * In der Praxis wird OPTIONS oft von Browsern oder Caches automatisch genutzt, um Informationen über Ressourcen zu erhalten.

### HTTP Status Codes und Statusnachrichten

* HTTP-Statuscodes sind dreistellige Codes, die den Erfolg oder Misserfolg einer HTTP-Anfrage anzeigen.
* Sie sind in fünf Klassen unterteilt:

<Table>
    <TableHeader>
        <TableColumn>Status Codes</TableColumn>
        <TableColumn>Nutshell</TableColumn>
        <TableColumn>Beschreibung</TableColumn>
        <TableColumn>Beispiel</TableColumn>
    </TableHeader>
    <TableBody>
        <TableRow>
            <TableCell>
                <InlineCode>1xx</InlineCode>
            </TableCell>

            <TableCell>
                <Text>Go on</Text>
            </TableCell>
            <TableCell>
                <Text>Informative Antworten, die den Fortschritt der Anfrage anzeigen.</Text>
            </TableCell>
            <TableCell>
                <Text>**100 Continue** - Der Client kann mit der Anfrage fortfahren.</Text>
            </TableCell>
        </TableRow>

        <TableRow>
            <TableCell>
                <InlineCode>2xx</InlineCode>
            </TableCell>

            <TableCell>
                <Text>Success</Text>
            </TableCell>
            <TableCell>
                <Text>Antworten auf erfolgreiche Anfragen.</Text>
            </TableCell>
            <TableCell>
                <div><Text>**200 OK** - Die Anfrage war erfolgreich und die Antwort enthält die angeforderten Daten.</Text></div>
                <div><Text>**201 Created** - Die Anfrage wurde erfolgreich verarbeitet und eine neue Ressource wurde erstellt.</Text></div>
            </TableCell>
        </TableRow>

        <TableRow>
            <TableCell>
                <InlineCode>3xx</InlineCode>
            </TableCell>

            <TableCell>
                <Text>Go away</Text>
            </TableCell>
            <TableCell>
                <Text>Weiterleitungen, die den Client anweisen, eine andere URL zu verwenden.</Text>
            </TableCell>
            <TableCell>
                <div><Text>**301 Moved Permanently** - Die angeforderte Ressource wurde dauerhaft verschoben.</Text></div>
                <div><Text>**302 Found** - Die angeforderte Ressource wurde vorübergehend verschoben.</Text></div>
            </TableCell>
        </TableRow>

        <TableRow>
            <TableCell>
                <InlineCode>4xx</InlineCode>
            </TableCell>

            <TableCell>
                <Text>You fucked up</Text>
            </TableCell>
            <TableCell>
                <Text>Codes, die auf einen Fehler des Clients hinweisen.</Text>
            </TableCell>
            <TableCell>
                <div><Text>**400 Bad Request** - Die Anfrage war ungültig oder konnte nicht verstanden werden.</Text></div>
                <div><Text>**404 Not Found** - Die angeforderte Ressource wurde nicht gefunden.</Text></div>
            </TableCell>
        </TableRow>

        <TableRow>
            <TableCell>
                <InlineCode>5xx</InlineCode>
            </TableCell>

            <TableCell>
                <Text>I fucked up</Text>
            </TableCell>
            <TableCell>
                <Text>Codes, die auf einen Fehler des Servers hinweisen.</Text>
            </TableCell>
            <TableCell>
                <div><Text>**500 Internal Server Error** - Ein unerwarteter Fehler ist auf dem Server aufgetreten.</Text></div>
                <div><Text>**503 Service Unavailable** - Der Server ist vorübergehend nicht verfügbar.</Text></div>
            </TableCell>
        </TableRow>
    </TableBody>
</Table>

### HTTP Header

* Header sind Metadaten einer HTTP-Anfrage oder -Antwort
* Sie begleiten den eigentlichen Inhalt (Body) und liefern Zusatzinformationen, z.B:
    * Wer sendet die Anfrage?
    * Welches Format hat der Inhalt?
    * Welche Caching Regeln gelten?
* Jeder Header ist ein Key-Value-Paar, das durch einen Doppelpunkt getrennt ist.
* Header sind case-insensitive (technisch), aber Konvention ist `My-Header-Name`
* Reihenfolge der Header ist nicht relevant
* Klassische Request-Header:
    * `Host`: Ziel-Hostname (z.B. www.example.com)
    * `User-Agent`: Informationen über den Client (z.B. Browser, Betriebssystem)
    * `Accept`: Akzeptierte Antwortformate (z.B. `application/json`, `text/html`, siehe MIME-Typen)
    * `Authorization`: Authentifizierungsinformationen (z.B. Token, Basic Auth)
* Klassische Response-Header:
    * `Content-Type`: Format des Inhalts (z.B. `text/html`, `application/json`)
    * `Content-Length`: Länge des Inhalts in Bytes
    * `Set-Cookie`: Setzt Cookies für den Client (siehe Cookies)
    * `Cache-Control`: Caching-Regeln für die Antwort
    * `Location`: Bei Weiterleitungen (3xx) die neue URL, zu der der Client umgeleitet werden soll

### HTTP Cookies

* Der Server kann den Client anweisen, sich bestimmte Informationen zu merken, die bei zukünftigen Anfragen automatisch mitgesendet werden sollen.
* Dies geschieht über den `Set-Cookie` Header in der HTTP-Antwort.
* Seltener kann auch der Client selbst Cookies setzen, die dann bei zukünftigen Anfragen an den Server gesendet werden.
* Der Client speichert diese Informationen und sendet diese zukünftig im `Cookie` Header bei Anfragen an den "gleichen" Host.
* Häufige Einsatzzwecke:
    * **Session-Management** (Login-Status, Warenkorb, Nutzer-ID, Access-Tokens)
    * **Personalisierung** (Spracheinstellungen, Nutzerpräferenzen)
    * **Tracking** (Analyse von Nutzerverhalten, Werbung)
* Unterschiede zu Headern
    * Cookies werden automatisch an den Host geschickt. Dies geschieht, ohne dass diese explizit im Request definiert werden müssen.
    * Cookies sind persistent, d.h. sie können über mehrere Sitzungen hinweg gespeichert werden.
    * Cookies können zusätzliche Attribute haben, die ihr Verhalten steuern, z.B. `HttpOnly`, `Secure`, `SameSite`.
    * Cookies können sicherer sein, da bspw. `HttpOnly` verhindert, dass JavaScript auf den Cookie zugreifen kann, was XSS-Angriffe erschwert.
* Technische Funktionsweise
    * Setzen eines Cookies:
        * Serverseitig: HTTP-Header `Set-Cookie: name=value; Path=/; HttpOnly; Secure; SameSite=Strict`
        * Clientseitig: JavaScript `document.cookie = "name=value; Path=/; Secure; SameSite=strict";`
        * Erklärung der Bestandteile:
            * `name=value`: Name und Wert des Cookies
            * `Path`: Der Pfad, für den das Cookie gültig ist (z.B. `/` für die gesamte Domain)
            * `HttpOnly`: Das Cookie ist nur über HTTP(S) zugänglich, nicht über JavaScript
            * `Secure`: Das Cookie wird nur über HTTPS-Verbindungen gesendet
            * `SameSite`: Einschränkung, wie das Cookie bei Cross-Site-Anfragen gesendet wird (z.B. `Strict`, `Lax`, `None`, siehe Tabelle unten)
    * Senden eines Cookies:
        * Clientseitig: Bei jeder Anfrage an den gleichen Host sendet der Browser den Cookie im `Cookie` Header mit.
        * Beispiel: `Cookie: session=abc123; lang=de`
    * Lebensdauer:
        * **Session-Cookie** (ohne `Expires` oder `Max-Age`): Gültig bis zum Schließen des Browsers
        * **Persistent-Cookie** (mit Ablaufdatum): Gültig bis zum angegebenen Datum oder der angegebenen Zeitspanne
    * Scope:
        * **Domain**: Das Cookie ist für die angegebene Domain und alle Subdomains gültig.
        * **Path**: Das Cookie ist nur für den angegebenen Pfad und dessen Unterverzeichnisse gültig.

<Table>
    <TableHeader>
        <TableColumn>SameSite Attribut</TableColumn>
        <TableColumn>Verhalten</TableColumn>
        <TableColumn>Beispiel: Cookie gesetzt auf `https://beispielseite.de`</TableColumn>
        <TableColumn>Wird Cookie mitgesendet?</TableColumn>
    </TableHeader>
    <TableBody>
        <TableRow>
            <TableCell>
                <InlineCode>Strict</InlineCode>
            </TableCell>

            <TableCell>
                <Text>Nur bei echten Same-Site-Anfragen (gleiche registrierte Domain inkl. Subdomains). Keine Cross-Site-Requests, auch nicht bei Link-Klicks von einer anderen Site.</Text>
            </TableCell>

            <TableCell>
                <Text>Benutzer ist auf `https://andere-seite.de` und klickt Link zu `https://beispielseite.de`</Text>
            </TableCell>

            <TableCell>
                <Text>Nein</Text>
            </TableCell>
        </TableRow>


        <TableRow>
            <TableCell/>
            <TableCell/>

            <TableCell>
                <div><Text>Benutzer ist auf `https://subdomain.beispielseite.de` und klickt Link zu `https://beispielseite.de`</Text></div>
            </TableCell>

            <TableCell>
                <Text>Ja</Text>
            </TableCell>
        </TableRow>


        <TableRow>
            <TableCell>
                <InlineCode>Lax</InlineCode>
            </TableCell>

            <TableCell>
                <Text>Cookies werden bei Top-Level-Navigationen (Link-Klicks, Eingabe in Adresszeile) gesendet, aber nicht bei eingebetteten Cross-Site-Requests (Bilder, Skripte, iframes, POSTs).</Text>
            </TableCell>

            <TableCell>
                <Text>Benutzer ist auf `https://andere-seite.de` und klickt Link zu `https://beispielseite.de`</Text>
            </TableCell>

            <TableCell>
                <Text>Ja</Text>
            </TableCell>
        </TableRow>


        <TableRow>
            <TableCell/>
            <TableCell/>

            <TableCell>
                <div><Text>`https://andere-seite.de` lädt ein Bild von `https://beispielseite.de`</Text></div>
            </TableCell>

            <TableCell>
                <Text>Nein</Text>
            </TableCell>
        </TableRow>


        <TableRow>
            <TableCell>
                <InlineCode>None</InlineCode>
            </TableCell>

            <TableCell>
                <Text>Cookies werden immer gesendet, auch bei Cross-Site-Requests – muss aber mit Secure kombiniert werden (nur über HTTPS).</Text>
            </TableCell>

            <TableCell>
                <Text>`https://andere-seite.de` lädt ein Bild von `https://beispielseite.de`</Text>
            </TableCell>

            <TableCell>
                <Text>Ja</Text>
            </TableCell>
        </TableRow>


        <TableRow>
            <TableCell/>
            <TableCell/>

            <TableCell>
                <div><Text>Benutzer ist auf `https://andere-seite.de` und klickt Link zu `https://beispielseite.de`</Text></div>
            </TableCell>

            <TableCell>
                <Text>Ja</Text>
            </TableCell>
        </TableRow>
    </TableBody>
</Table>

### Beispiel Anfrage und Antwort

```http request
POST /api/login HTTP/1.1
Host: www.beispielseite.de
User-Agent: Mozilla/5.0 (USS-Enterprise; starfleet-command/1701; Spock/LiveLongAndProsper)
Content-Type: application/json
Accept: application/json
Content-Length: 65
Cookie: session=abc123; lang=de

{
  "username": "max.mustermann",
  "password": "geheim123"
}
```

```http request
HTTP/1.1 200 OK
Date: Thu, 07 Aug 2025 14:10:00 GMT
Server: Klingon-Warbird/1.7.01 (Qo'noS; disruptors-armed; cloaking-device=active)
Content-Type: application/json
Content-Length: 113
Set-Cookie: session=xyz789; Path=/; HttpOnly; Secure; SameSite=Strict
Set-Cookie: lang=de; Path=/; Max-Age=604800

{
  "status": "success",
  "message": "Login erfolgreich",
  "userId": 42,
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
}
```